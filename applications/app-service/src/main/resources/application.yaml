server:
  port: 8086
spring:
  application:
    name: "crediya-solicitudes-ms"
  devtools:
    add-properties: false
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/crediya-solicitudes-ms_db
    username: rubenbarrios
    password:
#    url: "${SPRING_R2DBC_URL:r2dbc:postgresql://postgres-loan:5432/crediya-solicitudes-ms_db}"
#    username: "${SPRING_R2DBC_USERNAME:root}"
#    password: "${SPRING_R2DBC_PASSWORD:password}"
  sql:
    init:
      mode: "always"
    data:
      r2dbc:
        repositories:
          enabled: true
#adapters:
#  r2dbc:
#    host: "postgres-loan"
#    port: 5432
#    database: "crediya-solicitudes-ms_db"
#    schema: "public"
#    username: "root"
#    password: "password"

adapters:
  r2dbc:
    host: localhost
    port: 5432
    database: crediya-solicitudes-ms_db
    schema: public
    username: rubenbarrios
    password:

services:
  authentication:
    #url: "http://crediya-solicitudes-ms:8085"
    url: "http://localhost:8085"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
  health:
    circuitbreakers:
      enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8086"
jwt:
  secret: "mysuperlongsecretkeythatismorethan32bytes123!"
adapter:
  restconsumer:
    timeout: 5000
    url: "http://localhost:8085"
  sqs:
    region: "us-east-1"
    queueUrl: "https://sqs.us-east-1.amazonaws.com/090208085043/loan-application"
    # endpoint: "http://localhost:4566 # For localstack only"

resilience4j:
  circuitbreaker:
    instances:
      testGet:
        registerHealthIndicator: true
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: "2s"
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        minimumNumberOfCalls: 10
        waitDurationInOpenState: "10s"
      testPost:
        registerHealthIndicator: true
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: "2s"
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        minimumNumberOfCalls: 10
        waitDurationInOpenState: "10s"
